export default class ForumChannel {
  static async Process({
    userId,
    channelId,
    beforeDate,
    afterDate,
    passphrase,
    setAction,
    deleteMode,
    decryptMode,
    messageLimit,
  }) {
    return 0;
  }
}
